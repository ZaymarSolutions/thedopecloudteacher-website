name: Deploy Backend to Railway

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/railway-backend.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Validate Railway secrets and access
        run: |
          if [ -z "$RAILWAY_API_TOKEN" ]; then
            echo "RAILWAY API token secret is required"
            exit 1
          fi

          if [ -z "$RAILWAY_SERVICE_NAME" ]; then
            echo "RAILWAY_SERVICE_NAME secret is required"
            exit 1
          fi

          railway whoami
        env:
          RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_NAME: ${{ secrets.RAILWAY_SERVICE_NAME }}

      - name: Deploy to Railway
        run: |
          if [ -n "$RAILWAY_PROJECT_ID" ]; then
            RAILWAY_ENVIRONMENT_NAME=${RAILWAY_ENVIRONMENT:-production}
            railway up --project "$RAILWAY_PROJECT_ID" --environment "$RAILWAY_ENVIRONMENT_NAME" --service "$RAILWAY_SERVICE_NAME"
          else
            railway up --service "$RAILWAY_SERVICE_NAME"
          fi
        env:
          RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
          RAILWAY_SERVICE_NAME: ${{ secrets.RAILWAY_SERVICE_NAME }}
          RAILWAY_ENVIRONMENT: ${{ secrets.RAILWAY_ENVIRONMENT }}
        working-directory: ./backend
